func identity(x: i64) {
    return x;
}

func main() {
    var x: i32 = 1;       // x is the value of 1.
    var y: ptr<i32> = x; // x gets implicitly cast to a pointer type.
    var z: i32 = y;
    var w = (ptr<i32>)z;
    var q = identity(y);

    // var a: i32 = y + w;
    // var b = x + w;
    // var c = x + y;
}